require('./duvidaCruel');

/**
 * Se for criado o bundle através do comando 
 * ./node_modules/.bin/webpack em um terminal
 * qualquer ou abrindo o próprio terminal do 
 * node (ctrl+shift+'), irá criar um arquivo
 * bundle.js dentro da pasta public e ao
 * referenciar o arquivo './duvidaCruel', o
 * que tem dentro desse arquivo aparecerá
 * dentro do arquivo bundle.js
 */
